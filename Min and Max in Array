class Solution {
    public ArrayList<Integer> getMinMax(int[] arr) {
        // code Here
        int n = arr.length;
       Arrays.sort(arr);
        ArrayList<Integer> ans=new ArrayList<>();
                ans.add(arr[0]);
        ans.add(arr[n-1]);
        
                return ans;
            }
            
        }
